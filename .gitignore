# Go
/vendor/
*.exe
*.test
*.out
coverage.out
coverage.txt
dist/

# Nix
.direnv/
result
result-*

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Local development
.local/
*.local

# Plugin sockets
/tmp/bough-plugin-*.sock

# Instinct-gate denylist sidecar — names the very strings the guard
# exists to contain, so it must never be committed. Ship the template
# (docs/denylist.template.txt), keep the real list local.
/.bough/denylist.txt
